"use client";

import { CookieConsentWrapper as SharedCookieConsentWrapper } from "@ghxstship/ui";

/**
 * CookieConsentWrapper - Shared component using existing CookieConsentWrapper from UI package
 * Eliminates duplication by using centralized cookie consent component
 */
export const CookieConsentWrapper = SharedCookieConsentWrapper;

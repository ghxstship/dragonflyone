/**
 * @deprecated Use `Section` from foundations/layout instead.
 * SectionLayout is now an alias for Section for backward compatibility.
 */
import { Section, type SectionProps } from "../foundations/layout.js";

export type SectionLayoutProps = SectionProps;
export const SectionLayout = Section;
